import React from "react"
import Select from "./Select"

const SettingsScreen = () => (
    <main>
        <div className="row">
            <Select />
        </div>
    </main>
)

export default SettingsScreen